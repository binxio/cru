steps:
  - name: gcr.io/cloud-builders/docker
    args: [ 'build'
            '-t',
            'gcr.io/$PROJECT_ID/cru:$SHORT_SHA',
            '.']
  - name: gcr.io/cloud-builders/docker
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/cru:${TAG_NAME:-${BRANCH_NAME}}'
      - .
